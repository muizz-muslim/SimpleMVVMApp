<UserControl x:Class="SimpleMVVMApp.Views.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{DynamicResource BackgroundBrush}" Margin="20">
        <Border Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="15" BorderBrush="#444" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 🧠 Input and Buttons -->
                <StackPanel Grid.Row="0" Margin="0,0,0,15">

                    <!-- 🔍 Search Bar -->
                    <TextBlock Text="Search:" FontWeight="Bold" />
                    <TextBox Width="250" Margin="0,0,0,10"
                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Type to filter names" />

                    <!-- Name Input -->
                    <TextBlock Text="Name:" FontWeight="Bold" />
                    <TextBox Width="250" Margin="0,0,0,10"
                             Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Age Input -->
                    <TextBlock Text="Age:" FontWeight="Bold" />
                    <TextBox Width="250" Margin="0,0,0,10"
                             Text="{Binding NewAge, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                        <Button Content="Add"
                                Width="100" Height="30" Margin="0,0,10,0"
                                Background="#2196F3" Foreground="White" FontWeight="SemiBold"
                                BorderBrush="#1976D2"
                                Command="{Binding AddPersonCommand}"/>

                        <Button Content="Update"
                                Width="100" Height="30" Margin="0,0,10,0"
                                Background="#4CAF50" Foreground="White" FontWeight="SemiBold"
                                BorderBrush="#388E3C"
                                Command="{Binding UpdatePersonCommand}"/>

                        <Button Content="Clear"
                                Width="100" Height="30"
                                Background="#FFC107" Foreground="Black" FontWeight="SemiBold"
                                BorderBrush="#FFA000"
                                Click="ClearFields_Click"/>
                    </StackPanel>
                </StackPanel>

                <!-- 🧾 People List -->
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding FilteredPeople}" 
                         SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                         BorderBrush="#DDD" BorderThickness="1"
                         Padding="10" Background="#FAFAFA"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="5">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <StackPanel Width="250">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Age}" FontSize="12" Foreground="Gray"/>
                                    </StackPanel>

                                    <Button Content="Delete"
                                            Command="{Binding DataContext.DeletePersonCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Width="70" Height="25"
                                            Margin="10,0,0,0"
                                            Background="#E53935" Foreground="White"
                                            BorderBrush="#B71C1C"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
